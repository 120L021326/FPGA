# 一个 DSP 同时计算两个 BF16 乘法的原理

> [返回主线：FPGA 在 LLM 领域的应用调研](./FPGA调研.md)

## 1. 核心结论

论文中“一个 DSP 同时完成两个 BF16 乘法”的准确含义是：

> DSP 通过尾数打包，一次完成两个共享乘数的整数尾数乘法；符号、指数、规格化、舍入及特殊值处理仍由 DSP 外部逻辑完成。

这两个 BF16 乘法必须共享一个操作数：

\[
P_1=A_1B,\qquad P_2=A_2B
\]

在 DFVG 的多分支推测中：

- \(A_1,A_2\) 是两个 speculative branch 的 activation；
- \(B\) 是两个 branch 复用的同一个模型权重。

因此，这不是 DSP58 原生支持两路完整 BF16 浮点乘法，而是利用了：

\[
\boxed{
\text{多 branch 共享权重}
\Rightarrow
A_1B,\ A_2B
\Rightarrow
\text{尾数可打包后共同乘以 }B
}
\]

---

## 2. BF16 的尾数是多少 bit

一个 BF16 数共 16 bit：

| 字段 | 位数 | 含义 |
|---|---:|---|
| \(S\) | 1 bit | 符号位 |
| \(E\) | 8 bit | 指数字段，偏置为 127 |
| \(F\) | 7 bit | fraction，小数部分 |

对于规格化 BF16：

\[
x=(-1)^S(1.F)_2\,2^{E-127}
\]

虽然 BF16 中实际存储的 fraction 只有 7 bit，但规格化数的最高位 \(1\) 是隐含的。因此：

- 存储的 fraction：7 bit；
- 实际参与计算的 significand：\(1+7=8\) bit；
- 送入 DSP 做整数乘法的有效尾数：8 bit。

定义 8 bit 无符号整数尾数：

\[
M=2^7+F
\]

则：

\[
(1.F)_2=\frac{M}{2^7}
\]

例如：

\[
1.5=(1.1000000)_2
\]

其 fraction 为：

\[
F=1000000_2
\]

补上隐藏位后：

\[
M=11000000_2=192
\]

所以，更准确的表述是：

> BF16 存储 7 bit fraction，但规格化数的乘法使用 8 bit significand。

---

## 3. 单个 BF16 乘法如何计算

设两个规格化 BF16 数为：

\[
A=(-1)^{S_A}\frac{M_A}{2^7}2^{E_A-127}
\]

\[
B=(-1)^{S_B}\frac{M_B}{2^7}2^{E_B-127}
\]

则：

\[
AB=(-1)^{S_A\oplus S_B}
\frac{M_AM_B}{2^{14}}
2^{E_A+E_B-254}
\]

硬件可将 BF16 乘法拆成以下几个部分。

### 3.1 符号计算

\[
S_P=S_A\oplus S_B
\]

符号只需要一个异或操作。

### 3.2 尾数相乘

\[
R=M_AM_B
\]

两个有效尾数均为 8 bit，因此：

\[
8\text{ bit}\times8\text{ bit}
\rightarrow16\text{ bit}
\]

### 3.3 指数相加

未考虑尾数规格化时，结果的偏置指数为：

\[
E_P^{(0)}=E_A+E_B-127
\]

如果尾数乘积位于 \([2,4)\)，则需要将尾数右移一位，并使结果指数加一。

---

## 4. 两个尾数乘法如何打包进一个 DSP

现在需要同时计算：

\[
P_1=A_1B,\qquad P_2=A_2B
\]

取三个 BF16 规格化数的 8 bit 有效尾数：

\[
M_{A_1},\qquad M_{A_2},\qquad M_B
\]

将 \(M_{A_1}\) 和 \(M_{A_2}\) 打包到 DSP 的一个乘法输入中：

\[
M_{A,\mathrm{pack}}
=(M_{A_2}\ll16)+M_{A_1}
\]

位布局为：

| 打包输入位段 | 内容 |
|---|---|
| \([23:16]\) | \(M_{A_2}\)，8 bit |
| \([15:8]\) | 8 bit 间隔 |
| \([7:0]\) | \(M_{A_1}\)，8 bit |

DSP 的另一个乘法输入只放共享尾数 \(M_B\)。DSP 执行一次整数乘法：

\[
\begin{aligned}
R_{\mathrm{pack}}
&=M_{A,\mathrm{pack}}M_B\\
&=\left((M_{A_2}\ll16)+M_{A_1}\right)M_B\\
&=(M_{A_2}M_B\ll16)+M_{A_1}M_B
\end{aligned}
\]

由于：

\[
0\le M_{A_i},M_B\le255
\]

所以单个乘积最大为：

\[
255\times255=65025<2^{16}
\]

每个 \(8\times8\) 乘积最多占用 16 bit。把第二个乘积左移 16 bit 后，两个结果不会发生位重叠：

\[
R_1=R_{\mathrm{pack}}[15:0]
=M_{A_1}M_B
\]

\[
R_2=R_{\mathrm{pack}}[31:16]
=M_{A_2}M_B
\]

于是，一次 DSP 整数乘法得到两个独立的 16 bit 尾数乘积。

---

## 5. 具体数值例子

设：

\[
A_1=1.5,\qquad A_2=1.25,\qquad B=1.5
\]

它们的 8 bit 有效尾数分别为：

\[
M_{A_1}=11000000_2=\texttt{0xC0}
\]

\[
M_{A_2}=10100000_2=\texttt{0xA0}
\]

\[
M_B=11000000_2=\texttt{0xC0}
\]

### 5.1 打包两个 activation 尾数

\[
\begin{aligned}
M_{A,\mathrm{pack}}
&=(\texttt{0xA0}\ll16)+\texttt{0xC0}\\
&=\texttt{0xA000C0}
\end{aligned}
\]

### 5.2 DSP 执行一次乘法

\[
\texttt{0xA000C0}\times\texttt{0xC0}
=\texttt{0x78009000}
\]

### 5.3 拆出两个结果

低 16 bit：

\[
\begin{aligned}
R_1
&=\texttt{0x9000}\\
&=\texttt{0xC0}\times\texttt{0xC0}
\end{aligned}
\]

高 16 bit：

\[
\begin{aligned}
R_2
&=\texttt{0x7800}\\
&=\texttt{0xA0}\times\texttt{0xC0}
\end{aligned}
\]

由于两个输入尾数都按 \(2^7\) 缩放，乘积需要除以：

\[
2^7\times2^7=2^{14}
\]

因此：

\[
\frac{\texttt{0x9000}}{2^{14}}
=\frac{36864}{16384}
=2.25
\]

\[
\frac{\texttt{0x7800}}{2^{14}}
=\frac{30720}{16384}
=1.875
\]

对应：

\[
1.5\times1.5=2.25
\]

\[
1.25\times1.5=1.875
\]

---

## 6. DSP 输出后的规格化

规格化 BF16 的有效尾数范围为：

\[
1\le\frac{M}{2^7}<2
\]

因此两个有效尾数的乘积范围为：

\[
1\le\frac{R}{2^{14}}<4
\]

这会产生两种情况。

### 6.1 尾数乘积位于 \([1,2)\)

当：

\[
R[15]=0
\]

尾数不需要额外右移。未舍入前，结果的 8 bit 有效尾数主要取自：

\[
R[14:7]
\]

结果指数为：

\[
E_P=E_A+E_B-127
\]

### 6.2 尾数乘积位于 \([2,4)\)

当：

\[
R[15]=1
\]

需要将尾数右移一位：

\[
\frac{R}{2^{14}} =
\left(\frac{R}{2^{15}}\right)2^1
\]

未舍入前，结果的 8 bit 有效尾数主要取自：

\[
R[15:8]
\]

结果指数需要加一：

\[
E_P=E_A+E_B-127+1
\]

因此，两个乘积分别计算：

\[
E_{P_1}
=E_{A_1}+E_B-127+R_1[15]
\]

\[
E_{P_2}
=E_{A_2}+E_B-127+R_2[15]
\]

符号分别为：

\[
S_{P_1}=S_{A_1}\oplus S_B
\]

\[
S_{P_2}=S_{A_2}\oplus S_B
\]

随后，还要根据被截断的低位执行 BF16 舍入，通常采用 round-to-nearest-even。若舍入导致尾数再次溢出，还需要再次右移尾数并增加指数。

---

## 7. 为什么不能直接计算任意两个 BF16 乘法

这种方法适用于：

\[
A_1B,\qquad A_2B
\]

因为两个乘法共享 \(B\)，只需要打包 \(A_1,A_2\)。

如果希望同时计算：

\[
A_1B_1,\qquad A_2B_2
\]

并把两侧操作数都打包，则整数乘法会展开成：

\[
(A_2 2^s+A_1)(B_2 2^t+B_1) =
A_2B_2 2^{s+t}
+A_2B_1 2^s
+A_1B_2 2^t
+A_1B_1
\]

其中：

\[
A_2B_1,\qquad A_1B_2
\]

是并不需要的交叉项，而且通常无法通过简单位切片消除。

所以这种打包方法不能直接推广到两个完全独立的 BF16 乘法。

---

## 8. DSP 与外围逻辑的职责划分

DSP 内部主要完成：

\[
24\text{-bit packed mantissa}
\times
8\text{-bit shared mantissa}
\]

即两个共享乘数的 \(8\times8\) 尾数乘法。

DSP 外部的 LUT、FF 或其他专用逻辑仍需分别处理：

| 功能 | 处理内容 |
|---|---|
| 符号 | 分别计算 \(S_{A_1}\oplus S_B\) 和 \(S_{A_2}\oplus S_B\) |
| 指数 | 分别完成指数相加、减偏置和规格化修正 |
| 规格化 | 根据各自尾数乘积最高位决定是否右移 |
| 舍入 | 使用被截断位执行 BF16 舍入 |
| 特殊值 | 根据实现需求处理零、subnormal、无穷和 NaN |

因此，“一个 DSP 做两个 BF16 乘法”更准确的硬件表述是：

\[
\boxed{
\text{一个 DSP 尾数乘法器}
+
\text{两套轻量级浮点外围逻辑}
\rightarrow
\text{两路共享权重的 BF16 乘法结果}
}
\]

---

## 9. 为什么该优化适合 DFVG

普通单 token decode 中，同一权重通常只与一个 activation 相乘，缺少这种双路打包条件。

DFVG 同时生成多个 speculative branch。同一个权重 tile 可以连续作用于多个 branch activation：

\[
W[x_1,x_2,\ldots,x_B]
\]

因此，它同时获得了两类复用：

1. **存储器层面的权重复用**：同一个权重 tile 从 HBM 加载一次，服务多个 branch；
2. **DSP 内部的计算复用**：同一个权重尾数作为共享乘数，与两个打包的 activation 尾数相乘。

所以该优化并不是孤立的低精度乘法技巧，而是与 DFVG 的多分支执行方式共同设计的：

\[
\boxed{
\text{多分支形成 micro-batch}
\Rightarrow
\text{共享权重}
\Rightarrow
\text{尾数打包}
\Rightarrow
\text{提高单个 DSP 的有效乘法吞吐}
}
\]

## 10. 实现时需要注意的边界

- 上述推导主要针对规格化 BF16 数；
- 若系统需要完整 IEEE 语义，还要处理 subnormal、零、无穷和 NaN；
- 两个尾数乘积虽然在一个 DSP 内产生，但两路指数、符号和规格化状态仍然独立；
- 打包间隔必须足以容纳单个尾数乘积，避免进位污染相邻结果；
- 这种方法提高的是尾数乘法密度，整体加速仍取决于外围逻辑、数据供给和累加路径能否匹配；
- 在矩阵乘法中，乘积通常还要进入更高精度的累加器，不能把“两个 BF16 乘法结果”与“两个完整 BF16 MAC”混为一谈。
